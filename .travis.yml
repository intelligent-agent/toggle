matrix:
  include:
    - language: python
      python: 3.6
      install:
        - pip install -r requirements.txt
        - pip install yapf==0.25.0
      script:
        - yapf --diff --exclude venv/ --exclude .git/ -r .
        - pytest
